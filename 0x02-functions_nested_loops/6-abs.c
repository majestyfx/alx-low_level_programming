#include "main.h"
/**
 * _abs  - completes the value of
 * @c: number to complete
 * Return: absolute num. of zero
 */
int _abs(int c);
{
if (c < 0)
{
int abs_val;
abs_val = c = -1;
return (abs_val);
}
return (1);
}
