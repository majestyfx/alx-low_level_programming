#include "main.h"

/**
 * mul - checks for uppercase character
 * @a: first function to be checked
 * @b: second function to be checked
 * Return: 1 for capital letter or 0 for any ielse
 */

int mul(int a, int b)
{
return (a * b);
}
